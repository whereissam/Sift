"""Reaching the bytes: platform dispatch, downloading, existing-caption fetch.

The part that breaks when a platform changes its player, and the part nobody
else wants to maintain. Treat every module here as load-bearing.
"""
